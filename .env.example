# Copy to .env and fill in your values
# Per-vCenter credentials can also go in vcenters_list.txt
# These env vars are used as fallback defaults

# VCENTER_DEFAULT_USER=readonly@vsphere.local
# VCENTER_DEFAULT_PASSWORD=changeme
